version: "3.8"

services:
  app:
    image: superklok/currentcourantpointca:0.5.11
    build: 
      context: .
    ports:
      - "80:80"
    command: node app.js
    volumes:
      - .:/app
    environment:
      - NODE_ENV=production