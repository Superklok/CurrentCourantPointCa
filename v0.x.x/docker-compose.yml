version: "2.4"

services:
  app:
    image: currentcourantpointca:0.5.9
    build: 
      context: .
    ports:
      - "80:80"
    command: node app.js
    volumes:
      - .:/app
    environment:
      - NODE_ENV=development